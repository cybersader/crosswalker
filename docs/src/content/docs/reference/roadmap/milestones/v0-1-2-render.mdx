---
title: "v0.1.2 — render() v1 (folder + file + heading)"
description: "Implement the pure render(Recipe, ConceptIdentity) → Address function with folder/file/heading mechanisms wired. Tag and wikilink layout-mechanisms schema-reserved for v0.2."
sidebar:
  order: 12
  label: "v0.1.2 · render()"
tags: [milestone, v0-1, render, recipe, mechanisms, ch-22]
---

import { Aside } from '@astrojs/starlight/components';

## Goal

Implement the address-rendering function from [Ch 22](/crosswalker/agent-context/zz-log/2026-05-04-target-structure-synthesis/) §3 — the *single coupling point* between the [recipe](/crosswalker/concepts/terminology/) and the vault layout. Pure function; deterministic; vault-independent at Pass 1; produces an `Address` per the [v0.1 schema spec](/crosswalker/agent-context/v0-1-schema-spec/). Wires three of the five [mechanisms](/crosswalker/concepts/hierarchy-primitives/) (folder, file, heading); leaves tag and wikilink schema-reserved for v0.2.

## Status

✅ Done (2026-05-05). Pure `render(Recipe, ConceptIdentity) → Address` shipped with folder/file/heading mechanisms wired; tag/wikilink layout-mechanisms throw informative "deferred to v0.2" errors; 7-filter closed pipeline; 37 unit tests + 6 E2E tests (including determinism: 100 unit iterations + 50 E2E iterations all producing byte-identical output).

**Status last verified:** 2026-09-05 — scoped to the core render contract at commit `30aee63b`: `src/render/index.ts` still dispatches folder/file/heading and routes tag/wikilink to explicit deferral stubs, while `src/render/template.ts` still rejects unknown filters. The seven-filter list and test counts above describe the 2026-05-05 delivery slice; later additive filters exist, and no historical test count was rerun here.

## Dependencies

- [v0.1.1 — Type system + validation foundation](/crosswalker/reference/roadmap/milestones/v0-1-1-types-and-validation/) (typed `Recipe` and `ConceptIdentity` interfaces required)

**Blocks**: v0.1.3 (generation-engine integration) — without `render()`, the engine has no recipe-driven layout to call

## Scope

**In:**
- `render(recipe, identity) → Address` pure function
- Mechanisms wired: `folder`, `file`, `heading` (Ch 22 §10 v0.1 scope)
- Template grammar: R2RML-style `{var}` interpolation
- Filter set: `lower`, `upper`, `title`, `slug`, `tagsafe`, `fs-safe`, `truncate(N)` (Ch 22 §3.3)
- Templates referencing source-level scope variables (e.g. `{control.id}`, `{family.title}`)
- Pass 1 only — vault-independent, deterministic, hashable

**Out:**
- Mechanisms `tag` and `wikilink` as layout levels (validators warn "deferred to v0.2")
- `graph_edges` (validators warn "deferred to v0.2")
- Pass 2 link minimizer (`linkStyle: shortest`) — deferred to v0.3
- Auto-generated folder-tag-sync rules — Ch 22 §4.2; arrives with v0.2 tag mechanism

## Concrete tasks

- [ ] New `src/render/index.ts` — exports `render(recipe, identity, sourceScope) → Address`
- [ ] New `src/render/template.ts` — implements `{var}` interpolation + filter pipeline; closed filter set
- [ ] Per-mechanism modules: `src/render/mechanisms/folder.ts`, `file.ts`, `heading.ts`
- [ ] Stubs: `src/render/mechanisms/tag.ts`, `wikilink.ts` — throw "not yet implemented; coming in v0.2" when referenced by a recipe in v0.1
- [ ] Pass-1 invariant: same `(recipe, identity, sourceScope)` always produces byte-identical `Address` output
- [ ] Unit tests against the 3 worked NIST 800-53 examples in `spec/recipe.schema.json` (`nist-80053r5-allfolders`, `nist-80053r5-mostly-headings`, `nist-80053r5-hybrid`)
- [ ] Determinism test — call `render()` 1000 times with the same inputs; assert identical output every time
- [ ] Property test — for any valid recipe + identity, `Address.primary.path` survives JSON serialization round-trip

## Success criteria

- [ ] All 3 worked NIST examples produce expected `Address` outputs (paths, anchors, frontmatter)
- [ ] Determinism test passes — no hidden timestamps, no `Date.now()` leaking into render output
- [ ] Filter set closed — calling `{var|unknown_filter}` throws a recipe-validation error, not a silent miss
- [ ] Mechanism stubs (tag, wikilink) fail fast with informative error mentioning "v0.2"
- [ ] Test coverage > 90% for `src/render/`

## Files to touch

- `src/render/index.ts` — new
- `src/render/template.ts` — new
- `src/render/mechanisms/{folder,file,heading,tag,wikilink}.ts` — new
- `src/render/types.ts` — new (Address, RenderResult)
- `tests/render.test.ts` — new
- `tests/render-determinism.test.ts` — new

## Open questions

- [ ] How to scope `sourceScope` (the `{control.id}` / `{family.title}` resolution context)? Likely a `Map<string, unknown>` populated by the source iteration step (upstream of render)
- [ ] What does `render()` do when a template references an undefined variable? Throw vs. emit empty string vs. leave literal `{var}` in output

## Related

**Concept pages:**
- [Hierarchy primitives](/crosswalker/concepts/hierarchy-primitives/) — the 5 mechanisms (folder, file, heading, tag, wikilink) render() dispatches over
- [ETL and import (schema-as-primitive)](/crosswalker/concepts/etl-and-import/) — why render() is the only coupling point between recipe and vault
- [Terminology](/crosswalker/concepts/terminology/) — render(), Address, mechanism, template, ConceptIdentity, SourceScope
- [What makes Crosswalker unique](/crosswalker/concepts/what-makes-crosswalker-unique/) — the closed 5-mechanism grammar is the architectural distinguisher

**Agent context:**
- [v0.1 schema spec](/crosswalker/agent-context/v0-1-schema-spec/) — Address shape, recipe shape
- [Vision](/crosswalker/agent-context/vision/) — runtime-agnostic recipe schema; render() is the canonical reference implementation
- [Tradeoffs](/crosswalker/agent-context/tradeoffs/) — closed grammar vs. open extensibility

**Design decisions (synthesis logs):**
- [Ch 22 synthesis (target-structure expressivity)](/crosswalker/agent-context/zz-log/2026-05-04-target-structure-synthesis/) — recipe grammar + render() function spec; the design contract this milestone implements
- [Ch 23 synthesis (TS in-plugin engine)](/crosswalker/agent-context/zz-log/2026-05-04-bundle-engine-language-synthesis/) — why render() is TypeScript and runs in-plugin
- [v0.1 import-engine design (2026-05-04)](/crosswalker/agent-context/zz-log/2026-05-04-import-engine-design/) — design-phase summary
- [v0.1.2 delivery log (2026-05-05)](/crosswalker/agent-context/zz-log/2026-05-05-v0-1-2-render-shipped/) — what shipped + system-design diagram

**Research deliverables:**
- [Ch 22 deliverable (target-structure expressivity)](/crosswalker/agent-context/zz-research/2026-05-04-challenge-22-target-structure-expressivity/) — §3 has the formal render signature
- [Ch 23 deliverable (bundle/engine/language)](/crosswalker/agent-context/zz-research/2026-05-04-challenge-23-bundle-engine-language/) — chooses TS + AJV + JSONata
- [Ch 20 deliverable c (RML retargeted)](/crosswalker/agent-context/zz-research/2026-05-03-challenge-20-deliverable-c-rml-retargeted/) — R2RML lineage of the `{var|filter}` template grammar

**Spec & schema files:**
- [`spec/recipe.schema.json`](https://github.com/cybersader/crosswalker/blob/main/spec/recipe.schema.json) — layout_entry, mechanism, template `$defs`
- [`spec/tier1.schema.json`](https://github.com/cybersader/crosswalker/blob/main/spec/tier1.schema.json) — what render() ultimately produces

**Other milestones:**
- [v0.1.1 — Type system + validation foundation](/crosswalker/reference/roadmap/milestones/v0-1-1-types-and-validation/) — dependency
- [v0.1.3 — Generation engine integration](/crosswalker/reference/roadmap/milestones/v0-1-3-generation-engine-integration/) — what render() unblocks
- [v0.1.4 — Junction notes + crosswalk edges](/crosswalker/reference/roadmap/milestones/v0-1-4-junction-notes-and-crosswalks/) — kind dispatch extends render()
- [Milestone hub](/crosswalker/reference/roadmap/milestones/)
