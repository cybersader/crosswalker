---
title: "v0.1.3 — Generation engine integration"
description: "Refactor the generation engine to call render() instead of hardcoded layout. Wire the managed/user_preserve frontmatter merge semantics. Implement the _crosswalker provenance writer per spec/tier1.schema.json."
sidebar:
  order: 13
  label: "v0.1.3 · Engine integration"
tags: [milestone, v0-1, generation-engine, frontmatter, provenance]
---

import { Aside } from '@astrojs/starlight/components';

## Goal

Connect [`render()`](/crosswalker/reference/roadmap/milestones/v0-1-2-render/) to the existing import pipeline. After this milestone, the import wizard produces *spec-conformant* [Tier 1](/crosswalker/concepts/terminology/) output: every note is validated, every `_crosswalker` [provenance](/crosswalker/concepts/terminology/) block is correctly populated, and re-imports preserve user-edited frontmatter (`user_preserve` keys per [Ch 22 §8.4](/crosswalker/agent-context/zz-log/2026-05-04-target-structure-synthesis/#4-the-managed--user_preserve-frontmatter-split)) while overwriting recipe-managed keys.

## Status

✅ Done (2026-05-05). Generation engine refactored to call `render()` per row via `legacyConfigToRecipe()` Phase-0 compat shim; managed/user_preserve frontmatter merge wired into 'replace' mode; spec-conformant `_crosswalker` provenance via `buildProvenance`; path collision detection. 113 tests passing (85 unit + 28 E2E across 6 spec files including a full-flow test that imports a 3-row dataset, modifies a frontmatter field, re-imports, and verifies user-edited keys survived).

**Status last verified:** 2026-09-05 — scoped to the milestone's surviving engine contracts at commit `30aee63b`: both generation entry points remain, the legacy recipe shim, frontmatter merge and provenance builders remain active, and the engine still performs pre-write Tier 1 validation plus path/identity collision refusal. Later re-import and ownership work extends this historical slice; the 2026-05-05 acceptance count and wizard behavior were not rerun here.

## Dependencies

- [v0.1.1 — Type system + validation foundation](/crosswalker/reference/roadmap/milestones/v0-1-1-types-and-validation/)
- [v0.1.2 — render() v1](/crosswalker/reference/roadmap/milestones/v0-1-2-render/)

**Blocks**: v0.1.4 (junction notes); v0.1.5 (sidecar projector reads from this engine's output); v0.1.7 (exporters)

## Scope

**In:**
- Refactor `src/generation/generation-engine.ts` — replace hardcoded layout with `render()` calls per Ch 22
- `_crosswalker` provenance block writer (matches `spec/tier1.schema.json` `provenance_block` $def)
- `managed` / `user_preserve` frontmatter merge semantics (Ch 22 §8.4)
- Idempotent re-import: re-running a recipe overwrites managed keys, preserves user keys, preserves any non-managed-non-user keys (third-party-plugin-added frontmatter survives)
- Path collision detection — clear error when two concepts render to the same path
- Validation hook — call `validateTier1Frontmatter()` before write; abort the import on first invalid output

**Out:**
- Junction notes (v0.1.4)
- Crosswalk edges (v0.1.4)
- Tier 2 sidecar updates (v0.1.5)
- File-rename handling on recipe changes (defer; v0.2 may add a "rename detector")

## Concrete tasks

- [ ] Refactor `generation-engine.ts` so the inner per-row loop calls `render(recipe, identity, sourceScope)` instead of computing path/frontmatter inline
- [ ] New `src/generation/frontmatter-merge.ts` — pure function `merge(existing, recipe-managed, recipe-user_preserve_patterns) → newFrontmatter` with the three-key semantics (managed/preserved/third-party)
- [ ] Provenance writer — populates `_crosswalker.spec_version`, `source_ref`, `produced_at`, `producer`, `recipe`, `concept_cid`
- [ ] Source-content hashing — read source file, sha256, write to `_crosswalker.source_ref.source_hash`
- [ ] Recipe content hashing — sha256 of the recipe JSON (after AJV-canonicalization), write to `_crosswalker.recipe.hash`
- [ ] Concept identity hashing — `concept_cid` per Ch 22 §8.3 (sorted CURIEs + canonical attribute set + sorted relations)
- [ ] Validation hook — pre-write `validateTier1Frontmatter(fm)`; if invalid, abort the import with a clear error pointing at the offending row
- [ ] Path collision detection — track every emitted path during a generation pass; abort + report on collision
- [ ] Update wizard preview (Step 3) to use the same `render()` pipeline so preview matches actual output
- [ ] Tests: re-run a generation against a vault that has user-edited frontmatter; verify managed keys overwritten, user keys preserved, third-party keys preserved

## Success criteria

- [ ] Running `bun run fixtures` then doing an Import Wizard run against the resulting `test-vault/Frameworks/NIST-mini/` produces output identical to the fixtures (round-trip determinism)
- [ ] Re-running an import on a vault with user edits doesn't lose any user data
- [ ] Path collisions are reported clearly, not silently overwritten
- [ ] All output validates against `spec/tier1.schema.json`
- [ ] Every test in `tests/generation/*.test.ts` passes (existing + new)

## Files to touch

- `src/generation/generation-engine.ts` — major refactor
- `src/generation/frontmatter-merge.ts` — new
- `src/generation/provenance.ts` — new
- `src/generation/identity.ts` — new (CURIE + concept_cid computation)
- `src/import/import-wizard.ts` — Step 3 preview alignment
- `tests/generation/round-trip.test.ts` — new
- `tests/generation/preserve.test.ts` — new

## Open questions

- [ ] Should `concept_cid` include user-frontmatter keys? Per Ch 22 §8.4, no — user frontmatter is canonical state of the user-annotation domain, hashed separately. Confirm at implementation
- [ ] What's the user-facing message when a path collision is detected? Should we offer auto-suffix, or just abort?

## Related

**Concept pages:**
- [Terminology](/crosswalker/concepts/terminology/) — Tier 1, recipe, render(), provenance, managed/user_preserve, CURIE, concept_cid
- [ETL and import](/crosswalker/concepts/etl-and-import/) — schema-as-primitive; the engine is convenience around the schema contract
- [Ontology evolution](/crosswalker/concepts/ontology-evolution/) — re-import safety, idempotency, user-edit preservation
- [Hierarchy primitives](/crosswalker/concepts/hierarchy-primitives/) — what render() projects against
- [What makes Crosswalker unique](/crosswalker/concepts/what-makes-crosswalker-unique/) — round-trip determinism is a differentiator

**Agent context:**
- [v0.1 schema spec](/crosswalker/agent-context/v0-1-schema-spec/) — `_crosswalker` provenance_block, concept_note_frontmatter
- [Vision](/crosswalker/agent-context/vision/) — file-based canonical state; engine produces it
- [Tradeoffs](/crosswalker/agent-context/tradeoffs/) — strict pre-write validation vs. lenient

**Design decisions (synthesis logs):**
- [Ch 22 synthesis §4 (managed/user_preserve)](/crosswalker/agent-context/zz-log/2026-05-04-target-structure-synthesis/#4-the-managed--user_preserve-frontmatter-split) — frontmatter merge semantics
- [Ch 22 synthesis §10.7 (4-phase migration plan)](/crosswalker/agent-context/zz-log/2026-05-04-target-structure-synthesis/) — Phase 0 compat shim rationale
- [v0.1 import-engine design (2026-05-04)](/crosswalker/agent-context/zz-log/2026-05-04-import-engine-design/) — design-phase summary
- [Evidence-link edge model synthesis (Ch 07)](/crosswalker/agent-context/zz-log/2026-04-10-evidence-link-edge-model-synthesis/) — junction-note frontmatter shape that v0.1.4 builds on
- [v0.1.3 delivery log (2026-05-05)](/crosswalker/agent-context/zz-log/2026-05-05-v0-1-3-engine-integration-shipped/) — what shipped + system-design diagram

**Research deliverables:**
- [Ch 22 deliverable §8 (content addressing — target structure is a view)](/crosswalker/agent-context/zz-research/2026-05-04-challenge-22-target-structure-expressivity/)
- [Ch 09 deliverable (UUID/CWUUID)](/crosswalker/agent-context/zz-research/2026-05-02-challenge-09-uuid-cwuuid/) — concept_cid + identity hashing context

**Spec & schema files:**
- [`spec/tier1.schema.json` provenance_block](https://github.com/cybersader/crosswalker/blob/main/spec/tier1.schema.json) — `_crosswalker` shape
- [`spec/recipe.schema.json` frontmatter_block](https://github.com/cybersader/crosswalker/blob/main/spec/recipe.schema.json) — managed/user_preserve fields

**Other milestones:**
- [v0.1.1 — Type system + validation](/crosswalker/reference/roadmap/milestones/v0-1-1-types-and-validation/) — dependency
- [v0.1.2 — render() v1](/crosswalker/reference/roadmap/milestones/v0-1-2-render/) — dependency
- [v0.1.4 — Junction notes + crosswalk edges](/crosswalker/reference/roadmap/milestones/v0-1-4-junction-notes-and-crosswalks/) — what this unblocks
- [Milestone hub](/crosswalker/reference/roadmap/milestones/)
